name: ev_kalkulator
description: EV Charging Calculator
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0  # Ezt a sort adtuk hozzá!
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
